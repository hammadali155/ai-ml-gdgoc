APP_NAME="Dockerized API"
ENVIRONMENT="docker"
DEBUG=false
HOST="0.0.0.0"
PORT=8000
# Example key for local practice (never use real secrets in training repos)
API_KEY="docker-dev-key"
ALLOWED_ORIGINS="http://localhost:3000"
