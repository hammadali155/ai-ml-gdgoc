# AI/ML GDGoC Backend

## Overview

This repository contains a backend learning project that evolves from FastAPI basics into an AI-ready retrieval-augmented generation (RAG) system using Postgres, Qdrant, multilingual embeddings, and Groq-hosted LLMs.

## Current Scope

The project currently supports:

- FastAPI application with documented routes
- Environment-based settings via Pydantic Settings
- Automated tests with Pytest
- Docker and Docker Compose
- Postgres with SQLAlchemy and Alembic migrations
- Qdrant vector storage
- Multilingual embeddings (paraphrase-multilingual-MiniLM-L12-v2)
- Document chunk ingestion into Qdrant
- Retrieval over stored chunks (semantic search)
- RAG answers over retrieved chunks
- Roman-Urdu query normalization
- dual-query retrieval
- merged retrieval results for noisy inputs
- confidence-based decision control
- refusal when retrieval is too weak
- clarification when the question is underspecified
- Postgres logging of RAG request traces
- Controlled agent loop execution (plan -> retrieve -> judge -> answer)
- Logged agent traces in Postgres
## Tech Stack

| Layer         | Technology                          |
|---------------|-------------------------------------|
| Language      | Python 3.10+                        |
| Framework     | FastAPI                             |
| Config        | Pydantic Settings                   |
| Testing       | Pytest                              |
| Containers    | Docker / Docker Compose             |
| Database      | Postgres 17                         |
| ORM           | SQLAlchemy + Alembic                |
| Vector DB     | Qdrant                              |
| Embeddings    | Sentence Transformers               |
| LLM           | Groq (llama-3.1-8b-instant)         |
| Linting       | Ruff + pre-commit                   |

## Repository Structure

```
README.md
Dockerfile
docker-compose.yml
pyproject.toml
.gitignore
.env.example
.pre-commit-config.yaml
alembic.ini
src/
  fastapi_day4/         # Application code
    __init__.py
    api.py              # FastAPI routes
    settings.py         # Pydantic Settings config
    db.py               # SQLAlchemy engine/session
    models.py           # ORM models
    vector_store.py     # Qdrant client helpers
    ingestion.py        # Chunking and record building
    retrieval.py        # Semantic search over Qdrant
    normalization.py    # Roman-Urdu query normalization
    guardrails.py       # Decision logic and confidence scoring
    logging_utils.py    # Postgres logging helpers
    llm_client.py       # Groq LLM client helper
    rag.py              # RAG prompt construction & orchestration
    agent.py            # Agent loop (plan -> retrieve -> judge -> answer)
    schema.py           # Pydantic request/response models
tests/                  # Automated tests
scripts/                # Runnable helper scripts
data/
  raw/                  # Source documents for ingestion
  processed/            # Exported processed data
docs/
  screenshots/          # Evidence screenshots by day
alembic/                # Alembic migration files
```

## Local Setup

```bash
python3 -m venv .venv
source .venv/bin/activate
python -m pip install --upgrade pip
pip install -e ".[dev]"
pip install qdrant-client sentence-transformers groq
```

## Running the App

```bash
export PYTHONPATH=src
fastapi dev src/fastapi_day4/api.py
```

## Docker Compose Services

```bash
docker compose up -d --build
```

Services:

| Service   | Port  | Description               |
|-----------|-------|---------------------------|
| api       | 8000  | FastAPI application       |
| postgres  | 5432  | PostgreSQL 17 database    |
| qdrant    | 6333  | Qdrant vector database    |

## Ingestion Workflow

Documents are loaded from `data/raw/`, chunked into smaller passages (80 words, 20-word overlap), embedded using a multilingual sentence transformer model, and upserted into a Qdrant collection with metadata.

```bash
export PYTHONPATH=src
python scripts/day13_ingest_chunks.py
```

## Retrieval / Search Workflow

Query the ingested chunks by semantic similarity:

```bash
export PYTHONPATH=src
python scripts/day13_search_chunks.py
```

## RAG Endpoint

Send a question to the `/rag` endpoint and receive a grounded answer with source citations:

```bash
curl -X POST "http://127.0.0.1:8000/rag" \
  -H "Content-Type: application/json" \
  -d '{"question":"What does Qdrant do in this system?","limit":3}'
```

Or use the Swagger UI at [http://127.0.0.1:8000/docs](http://127.0.0.1:8000/docs).

The response includes:
- `question` — the original question
- `answer` — a grounded answer generated by the LLM
- `sources` — the retrieved chunks used as context

## Agent-RAG Endpoint

The `/agent-rag` endpoint extends the RAG flow with an explicit agent loop:

```bash
curl -X POST "http://127.0.0.1:8000/agent-rag" \
  -H "Content-Type: application/json" \
  -d '{"question":"What does Qdrant do in this system?","limit":3}'
```

The response includes the same fields as `/rag` plus an explicit `plan` array showing each step:

```json
{
  "plan": [
    "Normalize and inspect the question.",
    "Retrieve relevant chunks from Qdrant.",
    "Judge retrieval quality and decide whether to answer, clarify, or refuse.",
    "Generate an answer only if the evidence is sufficient."
  ],
  "action": "answer",
  ...
}
```

To inspect agent logs:
```bash
export PYTHONPATH=src
python scripts/day18_show_agent_logs.py
```

## Testing and Quality Tools

```bash
# Run tests
export PYTHONPATH=src
pytest

# Lint and format
ruff check .
ruff format .
```

## Environment Variables

| Variable                 | Description                        | Default                        |
|--------------------------|------------------------------------|--------------------------------|
| `APP_NAME`               | Application display name           | FastAPI App                    |
| `ENVIRONMENT`            | Deployment environment             | dev                            |
| `DEBUG`                  | Enable debug mode                  | false                          |
| `HOST`                   | API host                           | 127.0.0.1                      |
| `PORT`                   | API port                           | 8000                           |
| `API_KEY`                | API authentication key             | *(required)*                   |
| `DATABASE_URL`           | Postgres connection string         | *(required)*                   |
| `QDRANT_URL`             | Qdrant server URL                  | *(required)*                   |
| `QDRANT_COLLECTION_NAME` | Qdrant collection for chunks       | week2_day13_chunks             |
| `GROQ_API_KEY`           | Groq API key for LLM access        | *(required)*                   |
| `GROQ_MODEL`             | Groq model name                    | llama-3.1-8b-instant           |
| `ALLOWED_ORIGINS`        | CORS allowed origins (CSV)         | *(empty)*                      |
| `ENABLE_AGENT_LOOP`      | Enable agent loop workflow          | true                            |
| `MAX_AGENT_STEPS`        | Maximum steps in agent loop         | 4                               |
| `ENABLE_AGENT_LOGGING`   | Log agent traces to Postgres       | true                            |

## Progress by Day

- **Day 1–3:** Setup, GitHub workflow, Python project structure
- **Day 4–5:** FastAPI, config, Pydantic Settings
- **Day 6:** Testing with Pytest
- **Day 7:** Week 1 checkpoint
- **Day 8–9:** Docker, Dockerfile, Docker Compose
- **Day 10:** Postgres, SQLAlchemy, Alembic
- **Day 11:** Qdrant basics
- **Day 12:** Multilingual embeddings baseline
- **Day 13:** Ingestion pipeline and repository cleanup
- **Day 14:** Checkpoint (skipped in this workflow)
- **Day 15:** RAG endpoint v1 with Groq and cited sources
- **Day 16:** Roman-Urdu normalization and dual-query retrieval
- **Day 17:** guardrails, refusal/clarify behavior, and logging to Postgres
- **Day 18:** Agent loop with plan, retrieve, judge, and answer
## Known Limitations

- Chunking uses a simple word-count strategy (no sentence-aware splitting)
- No query normalization or preprocessing
- No confidence scoring or refusal logic
- No evaluation dataset for retrieval quality
- RAG v1 — may be slow due to retrieval + generation latency

## Next Steps

- Dockerizing the full final app cleanly
- preparing for deployment
- improving reliability and observability
- preparing the local-to-cloud transition
